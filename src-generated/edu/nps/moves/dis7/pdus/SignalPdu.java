/**
 * Copyright (c) 2008-2023, MOVES Institute, Naval Postgraduate School (NPS). All rights reserved.
 * This work is provided under a BSD-style open-source license, see project
 * <a href="https://savage.nps.edu/opendis7-java/license.html" target="_blank">license.html</a> and <a href="https://savage.nps.edu/opendis7-java/license.txt" target="_blank">license.txt</a>
 */
// header autogenerated using string template dis7javalicense.txt



package edu.nps.moves.dis7.pdus;

import java.util.*;
import java.io.*;
import edu.nps.moves.dis7.enumerations.*;
import com.google.common.primitives.*;
import com.google.common.base.Preconditions;
import java.nio.ByteBuffer;

/**
 * 5.8.4 Conveys the audio or digital data carried by the simulated radio or intercom transmission.
 * @see <a href="https://ieeexplore.ieee.org/document/6387564" target="_blank">IEEE Std 1278.1-2012, IEEE Standard for Distributed Interactive Simulation - Application Protocols</a> 
 */
public class SignalPdu extends RadioCommunicationsFamilyPdu implements Serializable, Marshaller
{

   /** The name of this PDU type */
   public static final String NAME = "SignalPdu";
   
   /** header is an undescribed parameter... */
   protected RadioCommsHeader  header = new RadioCommsHeader(); 

   /** encoding scheme used, and enumeration 
   Value space: uint16 */
   protected int encodingScheme;

   /** tdl type uid 178 */
   protected SignalTDLType tdlType = SignalTDLType.values()[0];

   /** sample rate 
   Value space: uint32 */
   protected UnsignedInteger sampleRate = UnsignedInteger.ZERO;

   /** length of data in bits 
   Value space: uint16 */
   protected int dataLength;

   /** number of samples 
   Value space: uint16 */
   protected int samples;

   /** list of eight bit values */
   protected byte[]  data = new byte[0]; 

   /** pad to 32-bit boundary */

   private byte[] padding = new byte[0];


/** Constructor creates and configures a new instance object */
 public SignalPdu()
 {
    setPduType( DisPduType.SIGNAL );
 }
/** copy method creates a deep copy of current object using preferred marshalling method
 * @return deep copy of PDU */
 public synchronized SignalPdu copy()
 {
     return copyDataOutputStream();
 }
/** Creates a "deep copy" of current object using ByteBuffer methods.
 * @return deep copy of PDU */
 public synchronized SignalPdu copyByteBuffer()
 {
     SignalPdu newCopy = new SignalPdu();
     ByteBuffer byteBuffer = ByteBuffer.allocate(400);
     try
     {
         this.marshal(byteBuffer);      // working
         newCopy.unmarshal(byteBuffer); // not working
     }
     catch (Exception e)
     {
         System.err.println("SignalPdu deep copy() marshall/unmarshall ByteBuffer exception " + e.getMessage());
         e.printStackTrace(System.err);
         System.exit(-1); // TODO: Abruptly ending VM not a good practice
     }
     return newCopy;
 }

/** byteArrayOutputStream (baos) is used for marshal/unmarshal serialization
   * @see copyDataOutputStream() */
protected ByteArrayOutputStream byteArrayOutputStream = new ByteArrayOutputStream();
/** dataOutputStream (dos) is used for marshal/unmarshal serialization
   * @see copyDataOutputStream() */
protected DataOutputStream      dataOutputStream      = new DataOutputStream(byteArrayOutputStream);

/** copy method creates a deep copy of current object using DataOutputStream methods.
 * @return deep copy of PDU */
 public synchronized SignalPdu copyDataOutputStream()
 {
     SignalPdu newCopy = new SignalPdu();
     try
     {
         this.marshal(dataOutputStream);
         byte[] byteArrayDOS = byteArrayOutputStream.toByteArray();
         newCopy.unmarshal(ByteBuffer.wrap(byteArrayDOS));
     }
     catch (Exception e)
     {
         System.err.println("SignalPdu deep copy() marshall/unmarshall DataOutputStream exception " + e.getMessage());
         e.printStackTrace(System.err);
         System.exit(-1); // TODO: Abruptly ending VM not a good practice
     }
        try
        {
                 dataOutputStream.flush();
            byteArrayOutputStream.flush();
            byteArrayOutputStream.reset();
        }
        catch (IOException ioe)
        {
            System.err.println("SignalPdu copyDataOutputStream() flush IOException: " + ioe.getMessage());
        }
     return newCopy;
 }

  /**
   * Returns size of this serialized (marshalled) object in bytes
   * @see <a href="https://en.wikipedia.org/wiki/Marshalling_(computer_science)" target="_blank">https://en.wikipedia.org/wiki/Marshalling_(computer_science)</a>
   * @return serialized size in bytes
   */
@Override
public synchronized int getMarshalledSize()
{
   int marshalSize = 0; 

   marshalSize = super.getMarshalledSize();
   if (header != null)
       marshalSize += header.getMarshalledSize();
   marshalSize += 2;  // encodingScheme
   if (tdlType != null)
       marshalSize += tdlType.getMarshalledSize();
   marshalSize += 4;  // sampleRate
   marshalSize += 2;  // dataLength
   marshalSize += 2;  // samples
   if (data != null)
       marshalSize += data.length * 1;
   if (padding != null)
       marshalSize += padding.length;

   return marshalSize;
}


/** Setter for {@link SignalPdu#header}
  * @param pHeader new value of interest
  * @return same object to permit progressive setters */
public synchronized SignalPdu setHeader(RadioCommsHeader pHeader)
{
    header = pHeader;
    return this;
}
/** Getter for {@link SignalPdu#header}
  * @return value of interest */
public RadioCommsHeader getHeader()
{
    return header;
}


/** Setter for {@link SignalPdu#encodingScheme}
  * @param pEncodingScheme new value of interest. Value space uint16
  * @return same object to permit progressive setters */
public synchronized SignalPdu setEncodingScheme(int pEncodingScheme)
{
    // Checking value is in value space uint16
    Preconditions.checkArgument(pEncodingScheme >= 0 && pEncodingScheme <= 65535, "Value outside valid value space");
    encodingScheme = pEncodingScheme;
    return this;
}
/** Getter for {@link SignalPdu#encodingScheme}
  * @return value of interest */
public int getEncodingScheme()
{
    return encodingScheme; 
}

/** Setter for {@link SignalPdu#tdlType}
  * @param pTdlType new value of interest
  * @return same object to permit progressive setters */
public synchronized SignalPdu setTdlType(SignalTDLType pTdlType)
{
    tdlType = pTdlType;
    return this;
}
/** Getter for {@link SignalPdu#tdlType}
  * @return value of interest */
public SignalTDLType getTdlType()
{
    return tdlType; 
}

/** Setter for {@link SignalPdu#sampleRate}
  * @param pSampleRate new value of interest. Value space uint32
  * @return same object to permit progressive setters */
public synchronized SignalPdu setSampleRate(UnsignedInteger pSampleRate)
{
    sampleRate = pSampleRate;
    return this;
}
/** Getter for {@link SignalPdu#sampleRate}
  * @return value of interest */
public UnsignedInteger getSampleRate()
{
    return sampleRate; 
}

/** Setter for {@link SignalPdu#dataLength}
  * @param pDataLength new value of interest. Value space uint16
  * @return same object to permit progressive setters */
public synchronized SignalPdu setDataLength(int pDataLength)
{
    // Checking value is in value space uint16
    Preconditions.checkArgument(pDataLength >= 0 && pDataLength <= 65535, "Value outside valid value space");
    dataLength = pDataLength;
    return this;
}
/** Getter for {@link SignalPdu#dataLength}
  * @return value of interest */
public int getDataLength()
{
    return dataLength; 
}

/** Setter for {@link SignalPdu#samples}
  * @param pSamples new value of interest. Value space uint16
  * @return same object to permit progressive setters */
public synchronized SignalPdu setSamples(int pSamples)
{
    // Checking value is in value space uint16
    Preconditions.checkArgument(pSamples >= 0 && pSamples <= 65535, "Value outside valid value space");
    samples = pSamples;
    return this;
}
/** Getter for {@link SignalPdu#samples}
  * @return value of interest */
public int getSamples()
{
    return samples; 
}

/** Setter for {@link SignalPdu#data}
  * @param pData new value of interest
  * @return same object to permit progressive setters */
public synchronized SignalPdu setData(byte[] pData)
{
    data = pData;
    return this;
}
/** Getter for {@link SignalPdu#data}
  * @return value of interest */
public byte[] getData()
{
    return data; 
}

/**
 * Serializes an object to a DataOutputStream.
 * @throws java.lang.Exception if something goes wrong
 * @see java.io.DataOutputStream
 * @param dos the OutputStream
 */
@Override
public synchronized void marshal(DataOutputStream dos) throws Exception
{
    super.marshal(dos);

    {
       header.marshal(dos);
       dos.writeShort((short) encodingScheme);
       tdlType.marshal(dos);
       dos.writeInt(sampleRate.intValue());
       // Count in bits (fixed + data + padding)
       int fixedAndVariableDataBits = 0 + (data.length * 8);
       int padBitsToInclude = 0;
       dos.writeShort(fixedAndVariableDataBits + padBitsToInclude);
       dos.writeShort((short) samples);

       for (int idx = 0; idx < data.length; idx++)
           dos.writeByte(data[idx]);

       padding = new byte[Align.to32bits(dos)];
    }
}

/**
 * Deserializes an object from a DataInputStream.
 * @throws java.lang.Exception if something goes wrong
 * @see java.io.DataInputStream
 * @see <a href="https://en.wikipedia.org/wiki/Marshalling_(computer_science)" target="_blank">https://en.wikipedia.org/wiki/Marshalling_(computer_science)</a>
 * @param dis the InputStream
 * @return marshalled serialized size in bytes
 */
@Override
public synchronized int unmarshal(DataInputStream dis) throws Exception
{
    int uPosition = 0;
    uPosition += super.unmarshal(dis);


    {
        uPosition += header.unmarshal(dis);
        encodingScheme = Short.toUnsignedInt(dis.readShort());
        uPosition += 2;
        tdlType = SignalTDLType.unmarshalEnum(dis);
        uPosition += tdlType.getMarshalledSize();
        sampleRate = UnsignedInteger.fromIntBits(dis.readInt());
        uPosition += 4;
        dataLength = Short.toUnsignedInt(dis.readShort());
        uPosition += 2;
        samples = Short.toUnsignedInt(dis.readShort());
        uPosition += 2;
        data = new byte[(((Number) dataLength).intValue() - 0 + 7) / 8];
        for (int idx = 0; idx < (((Number) dataLength).intValue() - 0 + 7) / 8; idx++)
            data[idx] = dis.readByte();
        uPosition += (data.length * 1);
        padding = new byte[Align.from32bits(uPosition,dis)];
        uPosition += padding.length;
    }
    return getMarshalledSize();
}

/**
 * Packs an object into the ByteBuffer.
 * @throws java.nio.BufferOverflowException if byteBuffer is too small
 * @throws java.nio.ReadOnlyBufferException if byteBuffer is read only
 * @see java.nio.ByteBuffer
 * @param byteBuffer The ByteBuffer at the position to begin writing
 * @throws Exception ByteBuffer-generated exception
 */
@Override
public synchronized void marshal(java.nio.ByteBuffer byteBuffer) throws Exception
{
   super.marshal(byteBuffer);
   header.marshal(byteBuffer);
   byteBuffer.putShort((short) encodingScheme);
   tdlType.marshal(byteBuffer);
   byteBuffer.putInt(sampleRate.intValue());
   // Count in bits (fixed + data + padding)
   int fixedAndVariableDataBits = 0 + (data.length * 8);
   int padBitsToInclude = 0;
   byteBuffer.putShort((short) (fixedAndVariableDataBits + padBitsToInclude));
   byteBuffer.putShort((short) samples);

   for (int idx = 0; idx < data.length; idx++)
       byteBuffer.put((byte)data[idx]);

   padding = new byte[Align.to32bits(byteBuffer)];
}

/**
 * Unpacks a Pdu from the underlying data.
 * @throws java.nio.BufferUnderflowException if byteBuffer is too small
 * @see java.nio.ByteBuffer
 * @see <a href="https://en.wikipedia.org/wiki/Marshalling_(computer_science)" target="_blank">https://en.wikipedia.org/wiki/Marshalling_(computer_science)</a>
 * @param byteBuffer The ByteBuffer at the position to begin reading
 * @return marshalled serialized size in bytes
 * @throws Exception ByteBuffer-generated exception
 */
@Override
public synchronized int unmarshal(java.nio.ByteBuffer byteBuffer) throws Exception
{
    super.unmarshal(byteBuffer);

    {
        header.unmarshal(byteBuffer);
        encodingScheme = Short.toUnsignedInt(byteBuffer.getShort());
        tdlType = SignalTDLType.unmarshalEnum(byteBuffer);
        sampleRate = UnsignedInteger.fromIntBits(byteBuffer.getInt());
        dataLength = Short.toUnsignedInt(byteBuffer.getShort());
        samples = Short.toUnsignedInt(byteBuffer.getShort());
        data = new byte[(((Number) dataLength).intValue() - 0 + 7) / 8];
        for (int idx = 0; idx < (((Number) dataLength).intValue() - 0 + 7) / 8; idx++)
            data[idx] = byteBuffer.get();
        padding = new byte[Align.from32bits(byteBuffer)];
    }
    return getMarshalledSize();
}


/**
 * Unpacks a Pdu into a PduMap from the underlying data.
 * @throws java.nio.BufferUnderflowException if byteBuffer is too small
 * @see java.nio.ByteBuffer
 * @see <a href="https://en.wikipedia.org/wiki/Marshalling_(computer_science)" target="_blank">https://en.wikipedia.org/wiki/Marshalling_(computer_science)</a>
 * @param byteBuffer The ByteBuffer at the position to begin reading
 * @return marshalled serialized size in bytes
 * @throws Exception ByteBuffer-generated exception
 */
public static PduMap fromBufferToMap(java.nio.ByteBuffer byteBuffer) throws Exception
{
    PduMap map;
    map = RadioCommunicationsFamilyPdu.fromBufferToMap(byteBuffer);

    map.put("header", RadioCommsHeader.fromBufferToMap(byteBuffer));
    map.put("encodingScheme", Short.toUnsignedInt(byteBuffer.getShort()));
    map.put("tdlType", SignalTDLType.unmarshalEnum(byteBuffer).getValue());
    map.put("sampleRate", UnsignedInteger.fromIntBits(byteBuffer.getInt()));
    map.put("dataLength", Short.toUnsignedInt(byteBuffer.getShort()));
    map.put("samples", Short.toUnsignedInt(byteBuffer.getShort()));
    // Valid primitive list varying length with bits
    byte[] data = new byte[(((Number) map.get("dataLength")).intValue() - 0 + 7) / 8];
    for (int idx = 0; idx < (((Number) map.get("dataLength")).intValue() - 0 + 7) / 8; idx++)
        data[idx] = byteBuffer.get();
    map.put("data", data);
    map.put("padding", new byte[Align.from32bits(byteBuffer)]);
    return map;
}

/**
 * Packs a Pdu represented in map into the ByteBuffer.
 * @throws java.nio.BufferOverflowException if byteBuffer is too small
 * @throws java.nio.ReadOnlyBufferException if byteBuffer is read only
 * @see java.nio.ByteBuffer
 * @param byteBuffer The ByteBuffer at the position to begin writing
 * @throws Exception ByteBuffer-generated exception
 */
public static void fromMapToBuffer(PduMap map, java.nio.ByteBuffer byteBuffer) throws Exception
{
    RadioCommunicationsFamilyPdu.fromMapToBuffer(map, byteBuffer);
    RadioCommsHeader.fromMapToBuffer((PduMap) map.get("header"), byteBuffer);
    byteBuffer.putShort(((Number) map.get("encodingScheme")).shortValue());
    SignalTDLType.getEnumForValue(((Number) map.get("tdlType")).intValue()).marshal(byteBuffer);
    byteBuffer.putInt(((Number) map.get("sampleRate")).intValue());
    byteBuffer.putShort(((Number) map.get("dataLength")).shortValue());
    byteBuffer.putShort(((Number) map.get("samples")).shortValue());

    byte[] data = (byte[]) map.get("data");
    for (int idx = 0; idx < data.length; idx++)
        byteBuffer.put(data[idx]);

    byte[] padding = new byte[Align.to32bits(byteBuffer)];
}

  /**
   * Returns size of this serialized (marshalled) object in bytes
   * @see <a href="https://en.wikipedia.org/wiki/Marshalling_(computer_science)" target="_blank">https://en.wikipedia.org/wiki/Marshalling_(computer_science)</a>
   * @return serialized size in bytes
   * @throws Exception   */
public static int getMarshalledSize(PduMap map) throws Exception
{
    int marshalSize = 0; 

    marshalSize += RadioCommunicationsFamilyPdu.getMarshalledSize(map);
    marshalSize += RadioCommsHeader.getMarshalledSize((PduMap) map.get("header"));
    marshalSize += 2;  // encodingScheme
    marshalSize += SignalTDLType.getEnumForValue(((Number) map.get("tdlType")).intValue()).getMarshalledSize();
    marshalSize += 4;  // sampleRate
    marshalSize += 2;  // dataLength
    marshalSize += 2;  // samples
    byte[] data = (byte[]) map.get("data");
    for (int idx = 0; idx < data.length; idx++)
        marshalSize += 1;
    marshalSize += ((byte[]) map.get("padding")).length;

    return marshalSize;
}

 /*
  * Override of default equals method.  Calls equalsImpl() for content comparison.
  */
@Override
 public synchronized boolean equals(Object obj)
 {
    if(this == obj)
      return true;

    if(obj == null)
       return false;

    if(!getClass().isAssignableFrom(obj.getClass())) //if(getClass() != obj.getClass())
        return false;

    return equalsImpl(obj);
 }

@Override
 public synchronized boolean equalsImpl(Object obj)
 {
     final SignalPdu rhs = (SignalPdu)obj;

     if( ! Objects.equals(header, rhs.header) ) return false;
     if( ! (encodingScheme == rhs.encodingScheme)) return false;
     if( ! (tdlType == rhs.tdlType)) return false;
     if( ! Objects.equals(sampleRate, rhs.sampleRate) ) return false;
     if( ! (dataLength == rhs.dataLength)) return false;
     if( ! (samples == rhs.samples)) return false;

     for (int idx = 0; idx < 0; idx++)
     {
          if(!(data[idx] == rhs.data[idx])) return false;
     }

    return super.equalsImpl(rhs);
 }

 @Override
 public synchronized String toString()
 {
    StringBuilder sb  = new StringBuilder();
    StringBuilder sb2 = new StringBuilder();
    sb.append(super.toString());
    sb.append(" header:").append(header); // writeOneToString
    sb.append(" encodingScheme:").append(encodingScheme); // writeOneToString
    sb.append(" tdlType:").append(tdlType); // writeOneToString
    sb.append(" sampleRate:").append(sampleRate); // writeOneToString
    sb.append(" dataLength:").append(dataLength); // writeOneToString
    sb.append(" samples:").append(samples); // writeOneToString
    sb.append(" data:");
    sb.append(Arrays.toString(data)); // writePrimitiveList
    sb.append(" padding:").append(padding); // writeOneToString

   return sb.toString();
 }

 @Override
 public int hashCode()
 {
	 return Objects.hash(this.header,
	                     this.encodingScheme,
	                     this.tdlType,
	                     this.sampleRate,
	                     this.dataLength,
	                     this.samples,
	                     this.data,
	                     this.padding);
 }
} // end of SignalPdu
